{
  "buildCommand": "cd client && npm ci --audit=false && npm run build",
  "outputDirectory": "client/build",
  "installCommand": "npm install --audit=false && cd client && npm install --audit=false",
  "env": {
    "NPM_CONFIG_AUDIT": "false",
    "NPM_CONFIG_FUND": "false"
  }
} 