{
  "buildCommand": "echo 'BUILD START' && date && cd client && npm ci --audit=false --silent && echo 'DEPS DONE' && date && npm run build && echo 'BUILD DONE' && date",
  "outputDirectory": "client/build",
  "installCommand": "echo 'INSTALL START' && date && npm install --audit=false --silent && cd client && npm install --audit=false --silent && echo 'INSTALL DONE' && date",
  "env": {
    "NPM_CONFIG_AUDIT": "false",
    "NPM_CONFIG_FUND": "false",
    "NPM_CONFIG_LOGLEVEL": "error"
  }
} 